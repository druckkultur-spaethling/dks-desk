# Webflow setzt den Mount-Pfad automatisch als BASE_URL.
# Für die Portal-API ist keine eigene Mount-Path-Variable erforderlich.

# Optional: echte KI-Auswertung von Bestell-PDFs.
# Ohne Schlüssel verwendet die App eine klar gekennzeichnete Vorführanalyse.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
